
var fibonacci = function(limit) {
    var sum = 0;
    var sequence = [];
    sequence.push(1);
    sequence.push(2);
    sequence.push(3);
    sequence.push(i);

    for(i = sequence[2] + sequence[1]; i < 4000000; i += ) {
        sequence.push(i);

        if(i % 2 == 0) {
            sum =+ i;
        } else {

        }
    }

    return sum;
}

fibonacci();